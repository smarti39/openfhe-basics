// ** Holds configuration parameters like file paths, default values, and any other constant values

#pragma once
#include <cstddef>

// The maximum depth of multiplications that can be performed on a ciphertext
const size_t MULT_DEPTH = 3;

// The length of facial template vectors included in the dataset 
const size_t VECTOR_DIM = 512;